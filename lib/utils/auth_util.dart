class AuthUtil {
  AuthUtil._();

  static Future<void> handleAfterLogout() async {
    // await FirebaseMessageService.dispose();
    // Constants.userInfo.value = DoctorModel.fromJson({});
    // FlutterLocalNotificationsPlugin().cancelAll();
    // Get.offAllNamed(AppRoutes.ADD_PHONE);
  }
}
